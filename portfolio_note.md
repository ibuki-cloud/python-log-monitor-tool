# Portfolio Note

## 30秒版

Pythonでログ監視ツールを作りました。  
ログファイルからWARNINGとERRORを抽出し、抽出結果を別ファイルに保存します。  
さらに件数と状態をJSON形式で出力し、実行履歴も残すようにしました。  
インフラ運用で必要になるログ確認をイメージして作成しました。

## 1分版

AWSとLinuxの学習の中で、サーバー運用ではログ確認が重要だと学びました。  
そこでEC2上でPythonを使い、ログファイルからWARNINGとERRORを抽出するツールを作成しました。

抽出した重要ログはimportant.logに保存し、WARNING件数、ERROR件数、合計件数、状態はimportant_summary.jsonにJSON形式で出力します。  
また、script.logに実行履歴も残すようにしています。

単なるPythonの文法練習ではなく、インフラ運用で行うログ確認や障害調査の初期対応を意識して作りました。  
Gitで管理し、GitHubにも公開しています。

## 学んだこと

- Pythonでファイルを読み込む方法
- 条件に合うログだけを抽出する方法
- 件数をカウントする方法
- JSON形式で結果を保存する方法
- GitHubで成果物を公開する流れ
- インフラ運用におけるログ確認の重要性
